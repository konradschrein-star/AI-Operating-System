os-usable-for-work — phase 7 deploy (round 17, task d4561713, run 3d5c6e98-5b92-4637-9edf-684c4b2c4e8b)
=======================================================================================================

PRE-DEPLOY  BUILD_ID : TsvXpNcIwjauqnJDJB2dy
POST-DEPLOY BUILD_ID : s5xDVZg4vNWFK40pXIcf6

They differ. That difference IS the proof of deployment; a command that exits 0 is not.


1. PRE-DEPLOY, read before anything was touched
-----------------------------------------------
$ cat /opt/forge-ai-os/forge-control-web/.next/BUILD_ID
TsvXpNcIwjauqnJDJB2dy

$ curl -s -L -m 10 http://127.0.0.1:7701/signin | grep -c "$(cat /opt/forge-ai-os/forge-control-web/.next/BUILD_ID)"
1

$ ss -lntp | grep 7701
LISTEN 0  511  *:7701  *:*  users:(("next-server (v1",pid=1901687,fd=23))


2. POST-DEPLOY, after the merge, the rebuild and the two allowed pm2 restarts
-----------------------------------------------------------------------------
$ cat /opt/forge-ai-os/forge-control-web/.next/BUILD_ID
s5xDVZg4vNWFK40pXIcf6

$ ss -lntp | grep -E '770[01]'
LISTEN 0  511  127.0.0.1:7700  0.0.0.0:*  users:(("node /opt/forge",pid=2965204,fd=31))
LISTEN 0  511          *:7701        *:*  users:(("next-server (v1",pid=2965178,fd=23))

Both PIDs are new (7701 was 1901687 before), and both are LISTENING — pm2 "online" is
not "listening" and was not accepted as evidence.


3. THE SERVED HTML — verbatim, the fragment carrying the new id
---------------------------------------------------------------
$ curl -s -L -m 10 http://127.0.0.1:7701/signin | grep -o 'self.__next_f.push(\[1,"0:{\\"P\\":null,\\"b\\":\\"[A-Za-z0-9_-]*\\"'

self.__next_f.push([1,"0:{\"P\":null,\"b\":\"s5xDVZg4vNWFK40pXIcf6\"

Occurrence counts in that one response body:

  new id (s5xDVZg4vNWFK40pXIcf6) : 1
  old id (TsvXpNcIwjauqnJDJB2dy) : 0

The old id is GONE from what the host serves. `/` 307s to `/signin` for an
unauthenticated request; `/signin` returns 200 (`curl -o /dev/null -w %{http_code} -L` → 200)
and embeds the id exactly once, which is why it is the fetch target.
