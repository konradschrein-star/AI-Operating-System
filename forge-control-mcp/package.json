{
  "name": "forge-control-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP stdio server exposing AI OS memory.search (vector + multi-hop GraphRAG + category filter) as a tool for cc-* workers.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
